---
correct: 4
---

# Scenario
A leading technology company is planning to build a document management portal that will utilize an existing Amazon DynamoDB table. The portal will be launched in Kubernetes and managed via AWS App Runner for easier deployment. The table has an attribute of DocumentName that acts as the partition key and another attribute called Category as its sort key. A DevOps Engineer was instructed to develop a feature that queries the DocumentName attribute yet uses a different sort key other than the existing one. To fetch the latest data, strong read consistency must be used in the database tier.

# Question
Which of the following solutions below should the engineer implement?

# Options
1. Add a Local Secondary Index that uses the DocumentName attribute and a different sort key.
2. Add a Global Secondary Index that uses the DocumentName attribute and a different sort key.
3. Add a Global Secondary Index which uses the DocumentName attribute and use an alternative sort key as projected attributes.
4. Set up a new DynamoDB table with a Local Secondary Index that uses the DocumentName attribute with a different sort key. Migrate the data from the existing table to the new table.

# Explanation
A Local Secondary Index (LSI) maintains an alternate sort key for the same partition key as the base table, letting you Query efficiently across that different dimension instead of scanning the whole table.

Unlike a GSI, an LSI supports strongly consistent reads, which the scenario requires. However, an LSI can only be created at the same time as the table — you cannot add one to an existing table.  

To create a Local Secondary Index, make sure that the primary key of the index is the same as the primary key/partition key of the table. Then you must select an alternative sort key that is different from the sort key of the table.

When you request a strongly consistent read, DynamoDB returns a response with the most up-to-date data, reflecting the updates from all prior write operations that were successful. A strongly consistent read might not be available if there is a network delay or outage. Strongly consistent reads are not supported on global secondary indexes.

The primary key of a local secondary index must be composite (partition key and sort key). A local secondary index lets you query over a single partition, as specified by the partition key value in the query.

Local secondary indexes are created at the same time that you create a table. You cannot add a local secondary index to an existing table, nor can you delete any local secondary indexes that currently exist.

1. Although it is using the correct type of index, you cannot add a local secondary index to an already existing table.
2. Although it is possible to query data without using a scan command, it is still not enough because GSI does not support strong read consistency which is required in the scenario.
3. A Local Secondary Index is the more appropriate fit here; projected attributes are just attributes stored in the index that can be returned by queries and scans performed on the index, hence, these are not useful in satisfying the provided requirement.
