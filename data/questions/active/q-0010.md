---
correct: 2,5
---

# Scenario
A company has multiple AWS accounts assigned to its development teams. As the company is projected to grow rapidly, management wants to consolidate all of its AWS accounts into a multi-account setup. To simplify the login process, management wants to utilize its existing directory service for user authentication.

# Question
Which combination of actions should a solutions architect recommend to meet these requirements? (Select TWO.)

# Options
1. Create an identity pool on Cognito and configure it to use the company's directory service. Configure AWS IAM Identity Center (AWS Single Sign-On) to accept Cognito authentication.
2. On the master account, use AWS Organizations to create a new organization with all features turned on. Invite the child accounts to this new organization.
3. Create Service Control Policies (SCP) in the organization to manage the child accounts. Configure AWS IAM Identity Center (AWS Single Sign-On) to use AWS Directory Service.
4. On the master account, use AWS Organizations to create a new organization with all features turned on. Enable the organization's external authentication and point it to use the company's directory service.
5. Configure AWS IAM Identity Center (AWS Single Sign-On) for the organization and integrate it with the company's directory service using the Active Directory Connector.

# Explanation
Use AWS Organizations to consolidate accounts. Use IAM Identity Center integrated with the existing directory via AD Connector for centralized authentication. AWS Organizations has no 'external authentication' feature. Cognito is for app SSO, not console federation. SCPs restrict permissions but aren't required for login.
