---
correct: 1,3
---

# Scenario
automatic DNS failover for its applications to a disaster recovery (DR) environment. 53 to automatically route to an alternate endpoint when the primary application stack in the us-west-1 region experiences an outage or degradation of service.

# Question
Select TWO

# Options
1. Set up health checks in Route 53 for non-alias records to each service endpoint. Configure the network access control list and the route table to allow Route 53 to send requests to the endpoints specified in the health checks.
2. Set up a record in Route 53 with a Weighted routing policy configuration. Associate the record with the primary and secondary record sets to distribute traffic to healthy service endpoints.
3. Use a Failover routing policy configuration. Set up alias records in Route 53 that route traffic to AWS resources. Set the Evaluate Target Health option to Yes, then create all of the required non-alias records.
4. Set up a CloudWatch Alarm to monitor the primary Route 53 DNS endpoint and create a custom Lambda function. Execute the ChangeResourceRecordSets API call using the function to initiate the failover to the secondary DNS record.

# Explanation
You can configure a health check that monitors an endpoint that you specify either by IP address or by domain name. Route 53 submits automated requests over the Internet to your application

2. Weighted routing simply lets you associate multiple resources with a single domain name (pasigcity.com) or subdomain name (blog.pasigcity.com) and choose how much traffic is routed to each resource. This can be useful for a variety of purposes, including load balancing and testing new versions of software.
4. you only have to use a Failover routing policy. Calling the Route 53 API is not applicable nor useful at all in this scenario.
