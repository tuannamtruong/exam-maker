---
correct: 1
---

# Scenario
A web application is deployed with Elastic Beanstalk using the "Rolling" deployment policy. It has an RDS DB instance attached.

# Question
prevent manually redeploy the old version.

# Options
1. Configure Immutable as the deployment policy in your Elastic Beanstalk environment for future deployments of your web application.
2. Implement a Blue/green deployment strategy in your Elastic Beanstalk environment for future deployments of your web application. Ensure that the RDS DB instance is still tightly coupled with the environment.
3. Configure Rolling with additional batch as the deployment policy in your Elastic Beanstalk environment for future deployments of your web application.

# Explanation
Immutable environment updates ensure that configuration changes that require replacing instances are applied efficiently and safely. If an immutable environment update fails, the rollback process requires only terminating an ASG. A failed rolling update, on the other hand, requires performing an additional rolling update to roll back the changes. When the first instance passes health checks, Elastic Beanstalk launches additional instances with the new configuration, matching the number of instances running in the original ASG. When all of the new instances pass health checks, Elastic Beanstalk transfers them to the original ASG, and terminates the temporary ASG and old instances.

2. A Blue/green deployment requires that your environment runs independently of your production database. This means that you have to decouple your RDS database from your environment. If your Elastic Beanstalk environment has an attached RDS DB instance, the data will be lost if you terminate the original (blue) environment.
3. This deployment type is just similar to rolling deployments and hence, this will not help alleviate the root cause of the issue in this scenario.
