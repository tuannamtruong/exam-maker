---
category: CloudTrail
---

# Front
To audit data access at all levels in CloudTrail, what event types are needed?

# Back
Both Management events (control-plane API calls like CreateBucket) AND Data events (object-level operations like S3 GetObject, Lambda Invoke). 

By default, trails log management events. Data events are off billed extra.
